# OracleApex-Field-Highlighter-Plugin
Field Highlighter is a Dynamic Action plugin for Oracle APEX that programmatically highlights one or more page items with a colored border, background tint, and optional pulsing animation. Use it to direct user attention to fields that need review, have validation issues, or are required under certain conditional logic.
